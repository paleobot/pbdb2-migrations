## 1. Dictionaries

- [ ] 1.1 Snapshot the reference schema's inline `bookType` enum (`["monograph","compendium","Ph.D. thesis","M.S. thesis","guidebook"]`) into `payloadSchemas/tests/fixtures/legacy-enums.json` as `book_types.name`, before `reference.schema.js` is rewritten; add no `languages` entry (D5)
- [ ] 1.2 In `postgresql/create_new.sql`, rename `dictionaries.book_types.book_type` → `name`, leaving rows, order and `id` untouched
- [ ] 1.3 Create `dictionaries.languages` (`id integer GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY`, `name text NOT NULL UNIQUE`), seeded in legacy enum order with `Portuguese` in place of `Portugese` (D5)
- [ ] 1.4 Drop `dictionaries.reference_types` and `refs.reference_type_id` from `create_new.sql` (D1)
- [ ] 1.5 Update `DICTIONARY_TABLES` in `src/run-migrations.js`: remove `reference_types`, add `languages`, keeping the count at 22
- [ ] 1.6 Extend the seed-fidelity test to cover `book_types`, and confirm it asserts nothing about `languages` or `roles`

## 2. The reference source

- [ ] 2.1 Rewrite `payloadSchemas/reference.schema.js` as `referenceSource`: no envelope, no `allowDuplicate`, no `response` blocks, no example, no ghosted `editSchema`/`patchSchema`/`getPropertiesForPubType`, no `getSchema`, no hand-written per-type `description`; `$id .../reference.json`, `unevaluatedProperties: false`
- [ ] 2.2 Add the `PUBLICATION_TYPES` table with the allowed and required lists from the `payload-schema-variants` spec, including `editors` on `serial monograph`, `bookType` on `standalone book`, and `other` declared unrestricted (D2)
- [ ] 2.3 Generate from it: `publicationType`'s inline `enum`; top-level `properties` for the shared fields plus the union of every type's fields; and one `x-create` conditional per type with `required: ["publicationType"]` inside its `if`, and a `then` carrying the type's `required` and, unless unrestricted, `propertyNames: { enum: [shared, type fields] }` (D3)
- [ ] 2.4 Set base `required` to `publicationType` and `publicationYear`; put `title` in `x-create` `required` (D4)
- [ ] 2.5 Annotate `permid` readOnly + `x-storage { column: "permid" }`, `legacyIDs` readOnly; `bookType` → `x-enumFrom { book_types, name }`; `language` → `x-enumFrom { languages, name }`, keeping its `default`
- [ ] 2.6 Carry over the existing field constraints unchanged (`publicationYear` maxLength 4, `pages` `{first, last}` integers ≥ 1 both required, `doi` maxLength 80, the `maxLength`s on volume, number, publisher, editors and city)
- [ ] 2.7 Add `referenceSource` to `sources.test.js`: exports only the source, every variant compiles in strict mode, `patch-guard` blocks exactly `permid` and `legacyIDs`, no inline enum remains except `publicationType`
- [ ] 2.8 Unit-test the per-type rules from the spec scenarios: extras pass `db` and fail `in-create` naming the field; per-type required fields; `serial monograph` + `editors` passes; `other` accepts every declared field and still rejects an undeclared one; missing `publicationType` applies no `then`; `title` required on create only; `bookType` allowed only on `standalone book` and `other`

## 3. PBDB refs migration

- [ ] 3.1 In `src/refs-migration/migrate-refs.js`, drop the `reference_types` load and the `reference_type_id` column from the row, INSERT and `ON CONFLICT` update; keep the legacy mapping as an in-script table producing the jsonb `publicationType`
- [ ] 3.2 Map `Portugese` → `Portuguese` in `mapLanguage`, and make the target list match `dictionaries.languages`
- [ ] 3.3 Treat a blank `reftitle` as absent, and for `standalone book` / `edited collection` fall back to the trimmed `pubtitle` verbatim, logging each `reference_no` and the total (D9)
- [ ] 3.4 Resolve and compile the `db` variant of `referenceSource` before reading MariaDB; validate every built jsonb before writing; log `reference_no`, errors and payload and abort on failure (D7)
- [ ] 3.5 Keep PBDB extras as built; do not filter fields for PBDB refs

## 4. PBot refs migration

- [ ] 4.1 In `src/pbot-refs-migration/migrate-pbot-refs.js`, drop the `reference_types` load and the `reference_type_id` column from both the INSERT and the idempotent UPDATE
- [ ] 4.2 Apply the two aliases to the jsonb `publicationType`, and write `other` (with a warning for a non-null value) when the type is neither an enum value nor an alias
- [ ] 4.3 After mapping, keep only shared fields and the normalized type's allowed fields, reading the lists from `PUBLICATION_TYPES` rather than copying them; log each drop with `pbotID`, field and value, and log the totals (D6)
- [ ] 4.4 Resolve and compile the `db` variant before the GraphQL request; validate every built jsonb before writing; log `pbotID`, errors and payload and abort on failure

## 5. Audit and runner

- [ ] 5.1 Add a `reference` entry to `REGISTRY` in `src/audit-payloads.js`: table `refs`, column `reference`, source `referenceSource`, `versioned: true`, round-trip columns `['permid']`, no children (D8)
- [ ] 5.2 Update `src/tests/audit-payloads.test.js` for the new entity: registry versioned flag, `parseArgs` accepts `reference`
- [ ] 5.3 Update `src/tests/run-migrations-audit.test.js`: the full run audits `collection`, `specimen`, `person`, `reference`; `refs` and `pbot-refs` audit `reference`; the no-audited-table case uses `authorities` instead of `refs`

## 6. Verification

- [ ] 6.1 `npm test`: all suites pass, including seed fidelity against a rebuilt database
- [ ] 6.2 Snapshot every ref's `legacyIDs` and `reference` jsonb from localhost to a scratch file (needed by 6.7), then rebuild the localhost target from `create_new.sql` and run the full pipeline through `src/run-migrations.js`; every step's postconditions pass and the runner's audit is clean for all four entities
- [ ] 6.3 `refs` step: every built jsonb validates; the log reports 1,468 pubtitle title fallbacks; 541 PBDB refs have no `title`
- [ ] 6.4 `pbot-refs` step: 18 fields dropped from 15 refs, each logged with its `pbotID`; no PBot ref holds a `publicationType` outside the enum; the 2 PBot `other` refs keep their `publisher`
- [ ] 6.5 SQL checks: 93,944 refs; no `language = 'Portugese'`, 363 `Portuguese`; no `publicationType` outside the enum; `refs.reference_type_id` and `dictionaries.reference_types` do not exist
- [ ] 6.6 `node src/audit-payloads.js --round-trip`: zero violations and zero differences across collection, specimen, person and reference
- [ ] 6.7 Diff the rebuilt refs jsonb against the pre-change jsonb per `legacyIDs`: the only differences are the corrected languages, restored titles, aliased PBot types and dropped PBot fields; `permid` values are regenerated by the rebuild and not compared

## 7. Specs

- [ ] 7.1 Confirm the seven delta specs match what was implemented, amending them if implementation revealed anything different
- [ ] 7.2 Run `openspec validate convert-reference-payload-schema`
- [ ] 7.3 Sync deltas into `openspec/specs/` via `/opsx:sync` or at archive time
