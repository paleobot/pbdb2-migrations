Baseline captured 2026-08-31, BEFORE any rename edit.
Database: localhost pbdb (the only live migration target; see .env).

name_opinions, grouped by edge_class:
  concept    58729
  lineage   190414
  root      517284
  TOTAL     766427

dictionaries.namechange_reasons, grouped by edge_class:
  concept        4   (junior synonym, replaced by, invalid subgroup, nomen oblitum)
  lineage        6   (correction, reranked, recombination, assignment,
                      misspelling, historical misspelling)
  root           1   (original)
  TOTAL         11

Post-rename expectation (task 8.6):
  name_opinions:  'name' == 190414, 'root' == 517284, 'concept' == 58729,
                  'lineage' absent, TOTAL unchanged at 766427
  namechange_reasons: 'name' == 6, 'root' == 1, 'concept' == 4,
                  'lineage' absent, TOTAL unchanged at 11

NOT CAPTURED — derive_taxa() baseline (tasks 1.2/1.3):
  derive_taxa() is NOT installed in this database, and the `taxa` table does not
  exist. reset-opinions.sql (the script this change runs) builds only the Layer 1
  opinion tables and the three dictionaries -- it does not create `taxa` or any
  derive/rebuild function. Those live in postgresql/create_new.sql, which has never
  been run in full against this database.
  => Tasks 1.2, 1.3 and 8.7 cannot be executed as written. See tasks.md group 1
     and the note in design.md's Migration Plan. Resolution pending user decision.

--------------------------------------------------------------------------
RESOLUTION of the 1.2/1.3/8.7 blocker (same session):
derive_taxa() was absent from `pbdb`, but postgresql/create_new.sql builds it
cleanly on an empty database, so the verification was run as a TWO-DATABASE
diff instead of a before/after on one -- a stronger test:

  pbdb_rename_base   <- schema from `git show HEAD:postgresql/create_new.sql`
                        (pre-rename, 18 'lineage' literals)
  pbdb_rename_check  <- schema from the renamed working tree

Identical data loaded into both from `pbdb` (persons, refs, authorities,
assignment_opinions, validity_opinions verbatim; name_opinions with edge_class
transformed 'lineage'->'name' in flight for the check DB only).
Dictionaries verified aligned first: namechange_reasons ids 1..11 map to the
same reason tokens in both, taxonomy_ranks and nomenclatural_statuses md5-equal.

  derive_taxa() rows       : 515489 in BOTH
  md5 of (permid, original_permid, accepted_spelling_permid, concept_permid)
    pre-rename  : 672203d55947a25cb441df74ed08f8a1
    post-rename : 672203d55947a25cb441df74ed08f8a1
  => BYTE-FOR-BYTE IDENTICAL.
  (The two 76MB dumps themselves are not kept -- they are regenerable, and the
   md5 above is the verifiable claim. Reproduce with the recipe in this file.)

Non-triviality: 117,082 permids have original_permid != permid (lineage
union-find ran) and 158,051 have concept_permid != permid (concept union-find
ran), so the equality is not the trivial output of two empty derivations.

POST-MIGRATION ACTUALS on `pbdb` after reset + re-mint + migrate (task 8.4):
  concept 58729 | name 190414 | root 517284 | TOTAL 766427   <- matches baseline
  namechange_reasons: concept 4 | name 6 | root 1            <- matches baseline
